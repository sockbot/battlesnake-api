const assert = require("chai").assert;
const { setGridSize, setBlocked } = require("../boardState");

describe("#setGridSize", () => {
  it("returns a 3x3 grid", () => {
    // assert.strictEqual([1, 2, 3], 1);
  });
});

describe("#setBlocked", () => {
  it("sets blocked coordinates", () => {
    // assert etc
  });
});
